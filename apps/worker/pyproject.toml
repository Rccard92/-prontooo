[project]
name = "prontooo-worker"
version = "0.0.0"
description = "Ingestione ricette e volantini per èProntooo. Non espone HTTP: scrive su Postgres."
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]>=3.2.3",
    "httpx>=0.28.1",
]

[tool.uv]
package = false
